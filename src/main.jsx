import React from 'react';
import { createRoot } from 'react-dom/client';
import BiancoDefinitive from '../bianco-definitive.jsx';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BiancoDefinitive />
  </React.StrictMode>,
);
